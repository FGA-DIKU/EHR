### censor.py
If using dataframe can be simplified
`censor()`, `_identify_background()`, `_identify_it_tokenized()`   should be moved to functional.

### infer.py
Most can be moved to funcitonal

### truncate.py
Can use dataframe format to simplify
Most can be moved to functional
